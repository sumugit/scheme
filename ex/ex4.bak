(define rmsDouble
  (lambda (data)(
                 / (apply + (map (lambda (x)(* x x)) data)) (length data))
    ))

(define rms
  (lambda (data)(
                 sqrt (rmsDouble data))
          ))